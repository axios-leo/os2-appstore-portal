# 公共开发约定与框架说明

本资料包允许阅读公共接口、公共框架和必要相邻模块职责，不包含其他模块的私有源码。下列是同一提交的必要章节摘录；原文中的全仓构建命令不用于本包，请以根目录构建说明为准。

来源：`docs/DEVELOPMENT_GUIDE.md` 第 17–52 行，提交 `4a650bc5`。

## 2 你的工作长什么样

每个模块的代码分三层，**你只动第三层**：

1. `modules/<m>/include/os2/<m>/api.hpp` —— 契约面接口。**只读**。想改 → 提"设计质疑/RFC"（AGENTS.md §上行反馈）。
2. `modules/<m>/src/<m>_service.hpp` —— 框架服务类，主题接线与流程骨架。**只读**（框架缺陷同样走 RFC）。
3. `modules/<m>/src/impl/` —— **你的目录**。继承框架类、重写 `protected virtual` 扩展点：

```cpp
// modules/scheduler/src/impl/wcet_scheduler.hpp（示例）
#include "scheduler_service.hpp"

namespace os2::scheduler::impl {
class WcetScheduler : public SchedulerService {
 public:
  using SchedulerService::SchedulerService;
 protected:
  std::unique_ptr<IPlanner> make_planner() override {
    return std::make_unique<WcetPlanner>();   // 你的实现
  }
};
}  // namespace os2::scheduler::impl
```

配套要求：`tests/` 里每个扩展点至少一个单测（用 `os2/platform/testing.hpp`）；
本模块 `CMakeLists.txt` 可加 `add_executable/add_test`，但不得引用其他模块目录。

## 3 必须遵守的运行时规矩

- **通信只走 `IBus` + `contracts.hpp` 的主题常量**。禁止自建 socket/共享内存/直接函数调用他模块（进程内拿到别的服务指针也不许调，单测桩除外）。
- **trace_id 全链路透传**：收到的消息里有 trace_id，就必须带到你发出的每条消息与日志。
- **日志用 `log()`**，结构化字段齐全；错误必须带契约错误码（`errc::*`）。
- **错误码不许私造**：需要新码 → 先提契约变更（error_codes.yaml）→ 镜像更新 → 再使用。`make contracts-check` 会拦截。
- **证据先行**：你的功能做完的标志不是"能跑"，而是能在 E2E 证据面板 / 指标 / 审计里看到它的输出（《架构说明》§4.1）。
- **高影响路径不可绕过**：设备控制的 PolicyCheck+互锁、制品激活的 PolicyCheck 已由框架固化，禁止短路。


来源：`docs/DEVELOPMENT_GUIDE.md` 第 60–66 行，提交 `4a650bc5`。

## 5 常见问题

- **"我需要另一个模块还没实现的功能"** → 面向契约主题编程 + 在你的单测里 `serve()` 一个桩应答方（各模块 tests/ 有现成范例）。不等、不越界。
- **"框架扩展点不够用"** → 不要绕过框架 hack，提 RFC，架构师加接缝。接缝是设计资产。
- **"单测怎么隔离总线"** → 每个测试 new 一个 `BusPair::make_inproc()`，天然隔离。
- **"性能会不会被 Msg/kv 拖累"** → M0.5 不谈性能；传输与序列化换代在 PAL 接缝后面，你的代码不受影响。


来源：`docs/MODULE_BOUNDARIES.md` 第 6–12 行，提交 `4a650bc5`。

## 1 通用规则

- 每模块可写目录 = `modules/<m>/src/impl/`、`modules/<m>/tests/`、本模块 `CMakeLists.txt`、本模块 `README.md`。
- 一律只读：`contracts/`、`platform/`、他模块全目录、`api.hpp` 与 `<m>_service.hpp`（本模块的也一样——框架类归架构师）。
- 跨模块交互只走 `contracts.hpp` 主题；新主题 = 契约变更（RFC → L0）。
- `sim/`、`deploy/`、`.ci/`、根构建：`@os2/integration` 专属。


来源：`docs/FRAMEWORK.md` 第 27–64 行，提交 `4a650bc5`。

## 2 platform/ 组成

| 头文件 | 内容 | 关键约束 |
|---|---|---|
| `contracts.hpp` | Command/Reply/Event/Metric 四元组、错误码、数据字典枚举、§8.7 主题清单、Msg 载体与映射 | 契约镜像，`make contracts-check` 强制与 `contracts/` 同步；M1 改代码生成 |
| `bus.hpp` | `IBus`（publish/subscribe/serve/request/poll）+ `InProcBus` 参考实现 + `BusPair` 双总线 | IBus 是全系统唯一通信接缝；InProcBus 仅供测试/单机联调；`poll()` 是网络化实现的协作式收发泵（进程内为空操作），由运行器节拍驱动 |
| `service.hpp` | `Os2Service` 基类：生命周期状态机、注册、心跳、健康、事件/指标证据出口 | 模块不得绕开基类自行注册/发心跳 |
| `log.hpp` | 结构化 JSON Lines 日志（《技术规范》§12.3 十字段） | 所有日志必须带 trace_id（有则必传） |
| `chain.hpp` | 服务链模型（SC-XML 镜像）+ 解析器 + DAG 校验/拓扑序 | 契约层逻辑；调度算法不在此 |
| `config.hpp` | key=value 配置 + `OS2_*` 环境变量覆盖 | 运行配置权威生成者是 model-data-mgr |
| `testing.hpp` | 零依赖极简单测框架（OS2_TEST/OS2_ASSERT） | M1 可换 GoogleTest，宏名不变 |

## 3 关键架构决策

1. **单线程协作式执行模型。** 运行器循环调用各服务 `tick(now)`；InProcBus 同步派发。好处：确定性、可单步调试、单测无竞态。真实传输接入后模型不变——网络化总线**不自带线程**，入站报文由运行器按节拍调用 `IBus::poll()` 协作式排空（os2_node：10ms 泵节拍 × 100ms 业务节拍）。框架保证"模块代码不感知线程与传输"。
2. **消息 = `Msg{type, kv}`，模块不手拼字段。** typed struct（Command/Reply/…）↔ Msg 的映射集中在 contracts.hpp。切换到 DDS/IDL 序列化时模块代码零改动。
3. **双总线从 day 1 分离。** `BusPair{mgmt, biz}`；注册/心跳/告警/回执只走 mgmt，业务数据只走 biz（`os2.biz.*`）。隔离证明是考核证据（《架构说明》§7.1）。
4. **控制命令闭环固化在框架。** 设备控制路径 PolicyCheck→互锁→执行→Reply+状态事件 写死在 DeviceSupervisorService；安全不可达=从严拒绝。开发者只能改互锁规则与驱动，不能跳过步骤。
5. **每模块必须产出证据。** 基类提供 `emit_event/emit_metric`，ops-mgr 汇聚 + LogSliceExport 导出——对齐"可观测优先/证据化验收"。
6. **注册中心不可达时降级启动（Degraded），不阻塞拉起**，对齐《架构说明》§8.4 启动失败按策略降级的要求。
7. **跨实例互联 = PAL 传输适配器，请求应答桥建在 Pub/Sub 之上（M0.8，ADR-0007）。**
   `PalBus` 在适配器 `networked()` 时自动启用：`request()` 以 `__reply_to/__corr` 封装经
   Pub/Sub 送达远端，`serve()` 侧桥回执到请求方回执主题（`__os2.reply.<节点>.<序号>`）；
   本地应答方仍走内联快径。好处：ITransportAdapter 只需 send/pump 两个原语，任何传输
   （UDP/DDS/RDMA）接入即同时获得 Pub/Sub 与 Request/Reply 两种语义。单应答方语义由部署
   约束保证（每契约主题一个应答节点）。UDP 为尽力而为传输：心跳/指标天然容失，请求应答
   由超时+调用方策略兜底；可靠传输与背压属 M1 DDS 适配器。
8. **实例装配点统一在 `pal::impl::make_bus_pair(config, node)`。** `pal.transport=udp` →
   双平面各一条 UDP-QosPalBus（QoS 分级生效）；缺省 → 进程内总线。配置键
   `pal.{mgmt,biz}.{listen,peers}` 可被 `OS2_*` 环境变量覆盖（容器注入友好）。
9. **注册自愈收敛：启动顺序是优化项、不是正确性前提（M0.9，ADR-0008）。** 三条内建路径：
   ① 降级 rejoin——注册失败的服务按 `service.rejoin_ms`（缺省 3000）在 tick 中退避重试；
   ② 纪元广播——注册中心每次拉起生成新 epoch，随注册回执下发并按 `registry.announce_ms`
   （缺省 2000）周期广播 `RegistryAnnounce`，服务发现纪元变化即重注册（注册中心重启、
   目录丢失场景的收敛）；③ 目录键 = `service_id@node_id`——同服务多节点共存、租约按
   节点粒度摘除，目录应答对既有消费方兼容（service_ids 去重 + 最佳实例语义 +
   `instances.<sid>`）。全部在框架基类与注册中心框架类内建，模块代码零感知。
