# 03 应用商店与软件仓：开发任务书

负责人、第一里程碑日期、验收人：待负责人填写。源码基线 `4a650bc5b8f0f8013ce51f866e5b228eaacb9bbb`；契约 `1.23.0`。

## 目标（按用户确认的十二模块名单）

制品的准入、版本治理与发布决策者：管住“什么能进来、发哪个版本、怎么灰度、出事怎么回滚”。

## 现有基础与本次工作

台账与较新裁定包含端侧软件仓范围和逐制品策略；早期 README 的门户/端岸方向需要按当前任务筛选。

已复制原仓库完整模块目录（接口、框架、实现、测试、构建与 README），原文件字节保持一致。

- 定义制品准入、版本治理、发布/灰度/回滚策略及持久化状态。
- 将发布决策与计算承载执行分开，通过正式接口下发并确认结果。
- 以制品摘要和版本贯穿准入、激活、健康观察和回滚证据。

先跑已有回归、核对目标缺口，再补充实现。任务书写的是交付目标，不表示目标已经实现。最新用户范围高于历史 README 中“暂不在范围”的旧排期，但不能把尚未开发的目标记成既有能力。

## 责任边界

仓内签名工具不等于允许分发生产签名凭据；本模块不代替 EM 管理实际进程生命周期。

对接方：安全策略、承载管理和健康观察模拟端。

开发接缝：make_verifier()、on_activated() 及本期激活策略接缝。

框架／核心入口：`modules/appstore-portal/src/store_service.hpp`。

已有模块一般在 `src/impl/`、`tests/`、模块 CMake/README 中开发；公共平台、契约、API 与框架变更统一提接口/框架变更请求。承载模块没有同样的 `src/impl/` 布局，其新增后端与核心修改边界需由集成负责人明确。新建模块先评审 API、状态机与目录，不套用不存在的类名。

## 推荐实施顺序

1. 阅读本包 README、现状摘录、API、框架与原有测试；记录已覆盖与待补的目标。
2. 对照“接口与联调”给出字段、单位、状态、超时、幂等和失败语义；新增契约先协调统一版本。
3. 用包内公共 `BusPair::make_inproc()` 和原有测试中的 `serve()` 桩完成隔离开发。
4. 接实际后端，运行本模块正常、异常和恢复用例，保留证据；模拟通过与真后端通过分列。
5. 提交本模块改动及测试报告，由集成负责人在完整系统合入和回归。

## 交付物

- 本模块源码与变更清单、固定依赖版本、可重复构建说明。
- API/事件/状态机说明和新增契约变更请求。
- 正常、拒绝、超时、重试、恢复测试及原始日志/报文证据。
- 已知限制、未实现清单、实际后端环境信息；按“验收清单”逐条勾选。
