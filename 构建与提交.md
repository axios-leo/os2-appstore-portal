# 构建与提交

## 环境

目标：Linux/POSIX，建议 WSL Ubuntu 22.04+；GCC/g++ ≥ 9、CMake ≥ 3.16、GNU make、Python ≥ 3.9、PyYAML、OpenSSL 命令行。

代码使用 `unistd.h`、`sys/wait.h`、POSIX socket 和 `/tmp`，不能把纯 Windows 上生成资料视为编译通过。Ubuntu 已有包源时可用 `sudo apt-get install build-essential cmake python3 python3-yaml openssl` 准备依赖。

## 在本包根目录执行

```bash
python3 verify_package.py
python3 tools/gen_contracts.py --check
cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug
cmake --build build --parallel 2
ctest --test-dir build --output-on-failure
```

先在收到原包时校验哈希；正常开发会改变文件，之后哈希不一致是预期现象。不要覆盖原始交付清单来掩盖改动。


构建会注册公共平台测试及本模块 CMake 中的现有测试；不会构建整机、演示台或其他模块。


根 `CMakeLists.txt` 是本次新增的独立构建入口。模块原始 CMake、源码和测试未修改；`VERSION`、公共契约与生成头文件均来自固定提交。不要照历史 README 的全仓 `make e2e` 命令操作本包。

默认测试可能写 `/tmp/os2_*` 文件并使用本机回环网络端口。使用独立开发环境，各包测试顺序执行，避免同名临时文件/端口冲突。证书测试材料是原有合成测试夹具，不是生产身份。


打包工具自检：`python3 tools/os2_pack.py --selftest`。生成测试证书时显式使用 `sh tools/gen_certs.sh build/test-certs`；签名/验签脚本从本包根目录执行，并显式传入测试证书目录。


## 提交

交回自己的模块变更、测试日志和文档。公共 API/框架/契约变更单独列出；集成负责人核对版本后回收至主仓库，并执行完整跨模块测试。此目录未创建 Git 历史或远程仓库，提交地址待负责人填写。

本次资料整理完成了文件和依赖静态校验，实际执行过的 Python 检查见“资料检查结果”。本机没有可用的 Linux/WSL 构建环境，C++ 配置、编译、CTest 与实机联调均未执行，不能把静态检查当成构建成功。
