# 本次资料检查结果

检查日期：2026-09-16；固定源提交 `4a650bc5b8f0f8013ce51f866e5b228eaacb9bbb`。

- 46 个原仓库文件逐一比对 SHA-256：一致。
- 20 个 C++ 源/头文件、34 个引号形式 include：均能在包内解析。
- 根/模块 CMake 引用的 3 个可执行目标源文件：存在。
- 本模块原始目录完整性：通过。
- 未包含其他模块目录、Git 历史、完整仓库归档或符号链接；未发现所检查的私钥/Token 标记。安全扫描仅为限定模式检查，不作绝对无敏感内容保证。

| 已实际执行的 Python 检查 | 结果 | 原始日志 |
|---|---|---|
| `python3 tools/gen_contracts.py --check` | 通过 | [contract-mirror](检查日志/contract-mirror.log) |
| `python3 tools/os2_pack.py --selftest` | 通过 | [artifact-packer](检查日志/artifact-packer.log) |

这些检查验证资料完整性、静态包含关系和包内工具行为。**CMake 配置、C++ 编译、CTest、真实后端与目标验收均未执行**：当前 Windows 未安装可用 Linux/WSL 构建环境。单模块根构建入口已提供，需接收者在约定环境运行。新增目标不因资料检查通过而视为完成。
