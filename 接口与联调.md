# 应用商店与软件仓：接口与隔离联调

## 本模块需要核对的接口

- ArtifactPublish / ActivationCommand
- PolicyCheck 与承载交接接口
- 制品清单、摘要、签名、激活态和健康观测字段

## 随包权威输入

- `platform/include/os2/platform/contracts.hpp`：当前 C++ 消息、主题、错误码镜像。
- `contracts/topics/topics.yaml`、`contracts/data-dictionary/data_dictionary.yaml`、`contracts/error-codes/error_codes.yaml`、`contracts/idl/os2_common.idl`：同一版本的公共源定义。
- `docs/rfcs/`：与本职责有关的正式演进或草案原文。草案状态必须逐篇看，不能因为放在包内就视为已批准/已实现。
- 本模块 `include/` 为既有 C++ 接口；`tests/` 展示目前实际调用和模拟应答。数据管理、时间同步尚无正式模块 API。

公共契约文件保留完整内容，以维持生成镜像一致；其中会出现相邻模块的接口名称，不含其内部实现。不要从名称反推出能力已经交付。

## 对接模拟件与场景

- 合法与篡改制品、错误摘要/签名
- 策略允许/拒绝与承载成功/失败模拟
- 健康变化和回滚目标

现有模块的测试文件已经包含部分总线应答桩；其余是开发要求，不是本包新增的可执行模拟器。两项待建模块尚无模拟器实现。

## 接口设计/联审时逐项填写

| 项目 | 填写内容 |
|---|---|
| 身份与版本 | 主题/方法、接口版本、提供者、消费者、请求 ID、trace_id |
| 数据 | 输入输出、必填/可选、类型、单位、枚举、合法及非法示例 |
| 时间 | 时间戳来源、时间域、有效期、超时、时间质量、单调时钟或墙钟 |
| 一致性 | 幂等键、重试/重复行为、并发、状态转换、重启恢复 |
| 失败 | 错误码、拒绝原因、部分成功、补偿/释放、审计与观测 |
| 兼容 | 旧版本接受条件、迁移规则、未知字段/不支持能力处理 |

已有接口从随包源码提取真实字段，未定义项登记为接口变更；不得在本模块私造公共主题和错误码。
